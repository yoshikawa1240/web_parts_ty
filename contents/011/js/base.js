$(function () {
  $('.cont_parts_y_011_slider').slick({
    // autoplay: true,
    // 自動再生
    autoplaySpeed: 5000,
    // 自動再生のスライド切り替えまでの時間をミリ秒で設定。
    slidesToShow: 1,
    // 表示するスライド数を設定。
    slidesToScroll: 1,
    // スクロールするスライド数を設定。
  });
});